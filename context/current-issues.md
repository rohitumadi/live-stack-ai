1. after updating the label in node the node is getting shrunk and moved to center

2) node resize feature is not working properly

expected behavior

1. after updating the label, the updated label should be visible. The node should not get shrunk.

2. The resize feature on node should work smoothly and properly. Just the way it works in production grid applications like Excalidraw
